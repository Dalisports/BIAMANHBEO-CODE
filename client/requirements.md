## Packages
framer-motion | Beautiful page transitions and micro-interactions for a premium feel
date-fns | Date formatting for order creation times
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes efficiently

## Notes
- Relying on native Web Speech API (`SpeechRecognition` and `speechSynthesis`) for voice features.
- Defaulting speech recognition language to `vi-VN` to match the user's order scenarios from the context.
- Using `@shared/routes` and `@shared/schema` for API contracts and types.
